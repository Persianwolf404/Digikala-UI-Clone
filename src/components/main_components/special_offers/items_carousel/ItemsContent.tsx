import Image from "next/image";
import React from "react";

export default function CarouselItems({
  data,
}: {
  data: { url: string; title: string };
}) {
  return (
    <div className="flex user-select-none flex-col w-[160px] h-full border ">
    
    </div>
  );
}
